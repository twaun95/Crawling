CREATE TABLE `chosun2015` (
	`no` INT(20) NOT NULL AUTO_INCREMENT,
	`url` VARCHAR(100) NULL DEFAULT NULL,
	`newsDate` VARCHAR(100) NULL DEFAULT NULL,
	`newsTitle` VARCHAR(255) NULL DEFAULT NULL,
	`newsSubtitle` VARCHAR(10000) NULL DEFAULT NULL,
	`content` VARCHAR(10000) NULL DEFAULT NULL,
	PRIMARY KEY (`no`)
)
